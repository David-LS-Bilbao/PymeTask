package com.dls.pymetask.presentation.ajustes

/** Tres niveles simples para escalar tipografías globales de la app */
enum class TextScale(val factor: Float) {
    SMALL(0.9f),
    MEDIUM(1.0f),
    LARGE(1.15f)
}
